```c++
//高精度测运行时间函数.cpp
#include <cstdio>
#include <windows.h>

LARGE_INTEGER freq_;
LARGE_INTEGER begin_time;
LARGE_INTEGER end_time;

int main()
{
	QueryPerformanceFrequency(&freq_);
	QueryPerformanceCounter(&begin_time);
	
    // TODO
    
	QueryPerformanceCounter(&end_time);
	double ns_time=(end_time.QuadPart-begin_time.QuadPart)*1000000.0/
        freq_.QuadPart;
	printf("Total: %lf Microsecons.", ns_time);
}
```

